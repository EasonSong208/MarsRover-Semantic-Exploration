diff --git a/src/driver/controller/controller/odom_publisher_node.py b/src/driver/controller/controller/odom_publisher_node.py
--- a/src/driver/controller/controller/odom_publisher_node.py
+++ b/src/driver/controller/controller/odom_publisher_node.py
@@ -95,6 +95,9 @@
         self.declare_parameter('linear_correction_factor', 1.00)
         self.declare_parameter('linear_correction_factor_tank', 0.52)
         self.declare_parameter('angular_correction_factor', 1.00)
+        # Safety architecture: only the final gate may write controller/cmd_vel.
+        # Set true only for isolated legacy calibration workflows.
+        self.declare_parameter('accept_legacy_cmd_vel', False)
         self.declare_parameter('machine_type', os.environ['MACHINE_TYPE'])
         
         self.pub_odom_topic = self.get_parameter('pub_odom_topic').value
@@ -129,7 +132,8 @@
         self.pose_pub = self.create_publisher(PoseWithCovarianceStamped, 'set_pose', 1)
         self.create_subscription(Pose2D, 'set_odom', self.set_odom, 1)
         self.create_subscription(Twist, 'controller/cmd_vel', self.cmd_vel_callback, 1)
-        self.create_subscription(Twist, 'cmd_vel', self.app_cmd_vel_callback, 1)
+        if self.get_parameter('accept_legacy_cmd_vel').value:
+            self.create_subscription(Twist, 'cmd_vel', self.app_cmd_vel_callback, 1)
         self.create_service(Trigger, 'controller/load_calibrate_param', self.load_calibrate_param)
 
         self.create_service(Trigger, '~/init_finish', self.get_node_state)
diff --git a/src/bringup/launch/bringup.launch.py b/src/bringup/launch/bringup.launch.py
--- a/src/bringup/launch/bringup.launch.py
+++ b/src/bringup/launch/bringup.launch.py
@@ -62,6 +62,10 @@
 
     joystick_control_launch = IncludeLaunchDescription(
         PythonLaunchDescriptionSource(os.path.join(peripherals_package_path, 'launch/joystick_control.launch.py')),
+        launch_arguments={
+            # All application/joystick/Nav2 commands enter the safety gate here.
+            'remap_cmd_vel': '/cmd_vel',
+        }.items(),
     )
 
     wheel_slip_monitor_launch = IncludeLaunchDescription(
diff --git a/src/navigation/config/nav2_params.yaml b/src/navigation/config/nav2_params.yaml
--- a/src/navigation/config/nav2_params.yaml
+++ b/src/navigation/config/nav2_params.yaml
@@ -37,7 +37,8 @@
     z_max: 0.05
     z_rand: 0.5
     z_short: 0.05
-    scan_topic: scan
+    # Localization receives the same tilt/rotation-gated stream as SLAM.
+    scan_topic: scan_slam
     set_initial_pose: True
     initial_pose: 
       x: 0.0
@@ -167,14 +168,16 @@
         observation_sources: scan
 
         scan:
-          topic: /scan
+          # scan_safety_gate masks everything beyond the trusted 40 cm radius.
+          topic: scan_local
           data_type: "LaserScan"
           max_obstacle_height: 2.0
           clearing: True
           marking: True
-          raytrace_max_range: 0.50
+          inf_is_valid: True
+          raytrace_max_range: 0.40
           raytrace_min_range: 0.0
-          obstacle_max_range: 0.50
+          obstacle_max_range: 0.40
           obstacle_min_range: 0.20
       always_send_full_costmap: True
   local_costmap_rclcpp_node:
diff --git a/src/slam/config/slam.yaml b/src/slam/config/slam.yaml
--- a/src/slam/config/slam.yaml
+++ b/src/slam/config/slam.yaml
@@ -13,7 +13,8 @@
     odom_frame: odom
     map_frame: map
     base_frame: base_footprint
-    scan_topic: scan
+    # Only level, stable scans released by scan_safety_gate reach mapping.
+    scan_topic: scan_slam
     use_map_saver: true
     mode: mapping  #localization
 
diff --git a/src/slam/launch/include/slam_base.launch.py b/src/slam/launch/include/slam_base.launch.py
--- a/src/slam/launch/include/slam_base.launch.py
+++ b/src/slam/launch/include/slam_base.launch.py
@@ -14,7 +14,7 @@
     map_frame = LaunchConfiguration('map_frame', default='map')
     odom_frame = LaunchConfiguration('odom_frame', default='odom')
     base_frame = LaunchConfiguration('base_frame', default='base_footprint')
-    scan_topic = LaunchConfiguration('scan_topic', default='scan')
+    scan_topic = LaunchConfiguration('scan_topic', default='scan_slam')
 
     enable_save_arg = DeclareLaunchArgument('enable_save', default_value=enable_save)
     use_sim_time_arg = DeclareLaunchArgument('use_sim_time', default_value=use_sim_time)
@@ -35,6 +35,7 @@
             'map_frame': map_frame,
             'odom_frame': odom_frame,
             'base_frame': base_frame,
+            'scan_topic': scan_topic,
             },
         convert_types=True
     )
diff --git a/src/slam/launch/slam.launch.py b/src/slam/launch/slam.launch.py
--- a/src/slam/launch/slam.launch.py
+++ b/src/slam/launch/slam.launch.py
@@ -50,7 +50,8 @@
             'map_frame': map_frame,
             'odom_frame': odom_frame,
             'base_frame': base_frame,
-            'scan_topic': '{}/scan'.format(frame_prefix),
+            # The node is already pushed into robot_name; keep this relative.
+            'scan_topic': 'scan_slam',
             'enable_save': enable_save
         }.items(),
     )
diff --git a/src/slam/launch/include/robot.launch.py b/src/slam/launch/include/robot.launch.py
--- a/src/slam/launch/include/robot.launch.py
+++ b/src/slam/launch/include/robot.launch.py
@@ -37,7 +37,9 @@
     use_sim_time = 'true' if sim == 'true' else 'false'
 
     map_frame = '{}map'.format(frame_prefix) if robot_name == master_name else '{}/map'.format(master_name)
-    cmd_vel_topic = '{}/controller/cmd_vel'.format(topic_prefix)
+    # Final chassis input is controller/cmd_vel. Producers publish to cmd_vel
+    # and must pass through cmd_vel_safety_gate first.
+    cmd_vel_topic = '{}/cmd_vel'.format(topic_prefix)
     scan_raw = '{}/scan_raw'.format(topic_prefix)
     scan_topic = '{}/scan'.format(topic_prefix)
     odom_frame = '{}odom'.format(frame_prefix)
@@ -48,9 +50,11 @@
     if compiled == 'True':
         peripherals_package_path = get_package_share_directory('peripherals')
         controller_package_path = get_package_share_directory('controller')
+        safety_monitor_package_path = get_package_share_directory('safety_monitor')
     else:
         peripherals_package_path = '/home/ubuntu/ros2_ws/src/peripherals'
         controller_package_path = '/home/ubuntu/ros2_ws/src/driver/controller'
+        safety_monitor_package_path = '/home/ubuntu/ros2_ws/src/safety_monitor'
 
     controller_launch = IncludeLaunchDescription(
         PythonLaunchDescriptionSource(
@@ -65,6 +69,12 @@
             'imu_frame': imu_frame,
             'use_sim_time': use_sim_time,
         }.items()
+    )
+
+    safety_monitor_launch = IncludeLaunchDescription(
+        PythonLaunchDescriptionSource(
+            os.path.join(safety_monitor_package_path, 'launch/wheel_slip_monitor.launch.py')
+        )
     )
 
     depth_camera_launch = IncludeLaunchDescription(
@@ -114,6 +124,7 @@
             use_depth_camera_arg,
             action_name_arg,
             controller_launch,
+            safety_monitor_launch,
             depth_camera_launch,
             lidar_launch, 
             joystick_control_launch,
diff --git a/src/slam/package.xml b/src/slam/package.xml
--- a/src/slam/package.xml
+++ b/src/slam/package.xml
@@ -6,6 +6,8 @@
   <description>TODO: Package description</description>
   <maintainer email="1270161395@qq.com">ubuntu</maintainer>
   <license>TODO: License declaration</license>
+
+  <exec_depend>safety_monitor</exec_depend>
 
   <test_depend>ament_copyright</test_depend>
   <test_depend>ament_flake8</test_depend>
